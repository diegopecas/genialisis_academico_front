import { Routes } from '@angular/router';

/**
 * Rutas del dominio del producto.
 * En el NÚCLEO este arreglo permanece vacío.
 * Cada producto derivado (GENIALISIS, Psyncronia, ...) reemplaza este archivo
 * con sus propias rutas de dominio; app.routes.ts las integra automáticamente.
 * No agregar rutas del núcleo aquí.
 */
export const dominioRoutes: Routes = [];